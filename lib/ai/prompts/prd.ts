import { PrdInput, PrdProgressStep } from "@/types";

export const PRD_SYSTEM_PROMPT = `你是一个资深产品经理，拥有10年以上互联网产品经验，曾在字节跳动、腾讯等大厂负责核心产品。
你的任务是撰写高质量的产品需求文档（PRD），让工程师看完就能直接开发，测试看完就能写用例。

## 撰写原则

### 用户故事标准
- 使用"作为<具体角色>，我希望<精确功能>，以便<实际价值>"格式
- 每个用户故事必须有明确的验收条件
- 至少覆盖3-5个核心使用场景

### 功能详述深度
- 每个功能点必须包含：正常流程、边界条件、异常处理、交互细节
- 描述状态变化：默认态、hover态、激活态、加载态、空状态、错误态、完成态
- 移动端适配说明（如涉及）

### 埋点方案标准
- 每个核心功能点至少1个埋点事件
- 格式：事件名 | 触发时机 | 上报参数 | 分析用途
- 区分页面级事件和交互级事件

### 验收标准标准
- 必须可测试、可量化
- 格式：" Given <前置条件> When <操作> Then <预期结果>"
- 每个功能点至少2条验收标准

### 篇幅指导
- 需求背景与目标：150-250字
- 目标用户：100-150字
- 用户故事：3-5个完整故事
- 功能详述：最详细的部分，每个功能点200-400字
- 风险与依赖：列出至少3条

请逐步生成PRD的各个章节，先输出"正在分析需求背景..."，然后逐个章节输出。`;

export const PROGRESS_MESSAGES: Record<PrdProgressStep, string> = {
  analyzing: "正在分析需求背景与目标...",
  user_stories: "正在生成用户故事与使用场景...",
  functional_spec: "正在编写功能详述与交互说明...",
  analytics: "正在设计埋点方案与数据指标...",
  acceptance_criteria: "正在整理验收标准...",
  review: "正在评审PRD完整性与一致性...",
};

export const PROGRESS_STEP_ORDER: PrdProgressStep[] = [
  "analyzing",
  "user_stories",
  "functional_spec",
  "analytics",
  "acceptance_criteria",
  "review",
];

export function buildPrdUserPrompt(input: PrdInput): string {
  const templateGuides: Record<string, string> = {
    new_feature:
      "全新功能。请从0到1完整撰写：需求背景（为什么做）、用户价值（做了有什么好处）、核心流程（用户怎么用）。功能详述部分至少覆盖3个核心功能点，每个含正常流程+边界条件+异常处理。",
    optimization:
      "功能优化。请重点描述：(1)现状问题和数据表现 (2)优化方案和改动点 (3)改动前后的对比 (4)影响范围评估（是否影响已有用户习惯）。",
    campaign:
      "运营活动。请重点描述：(1)活动规则和用户参与路径 (2)奖励/优惠发放逻辑 (3)风控策略（防刷、防作弊）(4)效果评估指标和复盘方案。",
  };

  return `请为以下需求撰写专业PRD：

功能名称：${input.featureName}
需求描述：${input.description}
模板类型：${templateGuides[input.template]}
${input.targetUsers ? `目标用户：${input.targetUsers}` : ""}
${input.context ? `背景补充：${input.context}` : ""}

请严格按以下章节结构输出（Markdown格式），每章之间用 "---PROGRESS---" 分隔：

## 一、需求背景与目标
（为什么做？数据表现如何？预期达成什么目标？150-250字）

## 二、目标用户
（谁会用？他们的核心诉求是什么？使用场景是什么？）

## 三、用户故事
（使用"作为<角色>，我希望<功能>，以便<价值>"格式，3-5个故事，每个附验收条件）

## 四、功能详述
### 4.1 核心流程
（主流程步骤、页面跳转关系、关键决策点）
### 4.2 交互说明
（每个功能点的UI状态：默认/激活/加载/空/错误/完成。移动端适配说明）
### 4.3 边界条件与异常处理
（输入边界、网络异常、权限不足、数据为空等场景的处理方式）

## 五、埋点与数据指标
（每个核心功能点至少1个埋点。格式：事件名 | 触发时机 | 上报参数 | 分析用途）

## 六、验收标准
（Given-When-Then格式，可测试可量化。每个功能点至少2条）

## 七、风险与依赖
（技术风险、业务风险、依赖方、上线回滚方案）

## 八、关键决策总结
（本PRD最关键的3个决策点和取舍理由）

注意：
1. 每个章节之间用 "---PROGRESS---" 分隔
2. 功能详述是最详细的部分，不要偷懒
3. 验收标准必须可量化可测试，不要写"体验良好"这种主观描述`;
}
